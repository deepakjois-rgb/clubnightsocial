// TODO: addPlayer(sessionId: string, name: string) → insert into players table, return Player
// TODO: getPlayers(sessionId: string) → fetch all players for a session
// TODO: updatePlayerState(playerId: string, state: PlayerState) → update player state in Supabase
