// TODO: createMatch(sessionId: string, teamA: string[], teamB: string[], courtId: string) → insert into matches table, return Match
// TODO: completeMatch(matchId: string, winner: MatchSideType) → mark match COMPLETED, increment player stats
// TODO: getMatches(sessionId: string) → fetch all matches for a session
